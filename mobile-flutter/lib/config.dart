/// Vapi public key — designed for client-side use (ADR-5). Keep the secret key server-side (n8n).
const String vapiPublicKey = "YOUR_VAPI_PUBLIC_KEY";

/// Vapi assistant id (configured in the Vapi dashboard, see backend/VAPI_ASSISTANT.md).
const String vapiAssistantId = "YOUR_VAPI_ASSISTANT_ID";
