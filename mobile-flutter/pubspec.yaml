name: personal_agent_mobile
description: "Vapi voice + text construction PM assistant app (ElevenLabs voice engine)."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  firebase_core: ^4.14.0
  firebase_messaging: ^16.6.0
  flutter:
    sdk: flutter
  flutter_local_notifications: ^22.3.0
  flutter_secure_storage: ^11.0.0
  flutter_timezone: ^5.1.0
  google_sign_in: ^6.3.0
  http: ^1.6.0
  timezone: ^0.11.1
  vapi: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
