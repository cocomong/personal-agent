# NEXT STEPS — saved 2026-08-23 (from session)

## 1. n8n credentials (the ONLY blocker for the PM assistant) — ~5 min in UI

Open https://n8n2.ordrnow.com → Credentials → Add credential.

**Credential A — type: PostgreSQL, name EXACTLY: `Supabase PostgreSQL`**
- Host: `postgres` (compose service name, not localhost)
- Port: `5432`
- Database: `postgres`
- User: `postgres`
- Password: value of `POSTGRES_PASSWORD` in `/home/ubuntu/n8n-compose/.env` on the VPS
  (generated by Hermes; grab it there rather than chat history)
- SSL: **off** (traffic stays on the Docker network)

**Credential B — type: SMTP, name EXACTLY: `Gmail SMTP`**
- Host: `smtp.gmail.com`
- Port: `465`
- SSL/TLS: **on**
- User: the Gmail address (env `GMAIL_USER` currently `support.ordrnow@gmail.com`;
  change it in /home/ubuntu/n8n-compose/.env on VPS if a different sender is wanted)
- Password: 16-char Google **App Password** (Google → Security → 2-Step Verification → App passwords)

Workflows reference credentials **by name** → they auto-link the moment both exist.
No node edits needed. After creating them, re-run the voice gateway test call
(POST /webhook/voice/gateway with a Vapi toolCall) — it should hit Postgres and
return a spoken response. Verified status: webhook 200, Normalize + router run clean,
only the credential lookup fails today.

## 2. Hermes session DB swap (still pending — Hermes must be stopped)

Recovered DB ready: 1,832/1,915 messages, 15 sessions, FTS verified.
- 1. Quit this session: `/exit`
- 2. Stop dashboard: `kill 579103`
- 3. Run swap: `~/.hermes/swap-recovered-db.sh`
- 4. Start `hermes` again (session_search works after this)
- Original preserved as state.db.corrupt.pre-swap / state.db.corrupt.orig.bak
- Note: the tail of the session that did the recovery is not in the recovered DB (by design)

## 3. Deploy status (done this session)

- Postgres 17 co-located on 8GB Arm VPS, migrations 0001–0013 applied, company_profile = Ireh Construction
- n8n pinned 2.35.7 (was unpinned :latest / 2.32.6), upgrade runbook in backend/DEPLOY.md §8
- 5 workflows imported + active; webhooks: POST /webhook/voice/gateway, approve-estimate,
  webhook/estimate/approval, approve-change-order, webhook/change-order/approval
- Fixed: webhookId missing (clean /webhook/<path> now) + Normalize Tool Call `arguments`
  sandbox error (Set node → Code node, field renamed to toolArgs) — commits 13a707b, 17c6152
- Nightly backup cron installed (03:00, backup.sh → /home/ubuntu/backups/pm)
- GMAIL_USER env = support.ordrnow@gmail.com (changeable)
